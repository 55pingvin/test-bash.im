<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {{ encore_entry_link_tags('app') }}
    {% block stylesheets %}{% endblock %}
</head>
<body>
<div class="container">

    <nav class="navbar navbar-light bg-light mb-3">
        <a class="navbar-brand" href="{{ path('index') }}">My-BashIm</a>
        <a href="{{ path('top') }}" class="">{% trans %}Top 10{% endtrans %}</a>
        <a href="{{ path('post_new') }}" class="float-right btn btn-primary">{% trans %}New post{% endtrans %}</a>

    </nav>

    {% block body %}{% endblock %}
    {% block javascripts %}{% endblock %}
</div>
{{ encore_entry_script_tags('app') }}

</body>

</html>
